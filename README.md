# نظام تأهيل أدوات التنفيذ المحلية

نظام متكامل لتأهيل الشركات والمقاولين المحليين - المؤسسة الوطنية للنفط

## 📋 المتطلبات

- PHP >= 8.2
- Composer
- MySQL >= 8.0
- Node.js & NPM (لـ Vite)

## 🚀 التثبيت والإعداد

### 1. تثبيت Dependencies

```bash
composer install
npm install
```

### 2. إعداد البيئة

```bash
cp .env.example .env
php artisan key:generate
```

### 3. إعداد قاعدة البيانات

قم بتعديل ملف `.env`:

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=your_database_name
DB_USERNAME=your_username
DB_PASSWORD=your_password
```

### 4. تشغيل Migrations و Seeders

```bash
php artisan migrate
php artisan db:seed --class=RolePermissionSeeder
```

### 5. إنشاء Storage Link

```bash
php artisan storage:link
```

### 6. تجميع Assets

```bash
npm run build
# أو للتطوير
npm run dev
```

### 7. تشغيل الخادم

```bash
php artisan serve
```

## 👤 المستخدم الافتراضي

بعد تشغيل Seeder، يمكنك تسجيل الدخول بـ:

- **البريد الإلكتروني**: `superadmin@example.com`
- **كلمة المرور**: `password`

⚠️ **مهم**: قم بتغيير كلمة المرور فوراً بعد أول تسجيل دخول!

## 🎯 المميزات الرئيسية

### 1. Landing Page
- تصميم أزرق حديث مع عناصر 3D
- استعلام عن حالة الطلب
- معلومات عن المؤسسة الوطنية للنفط
- الأسئلة الشائعة
- خريطة موقع المؤسسة

### 2. تسجيل الشركات (Stepper Form)
- **الخطوة 1**: البيانات الأساسية
- **الخطوة 2**: البيانات القانونية (PDF فقط)
- **الخطوة 3**: البيانات الفنية (PDF فقط)
- **الخطوة 4**: البيانات المالية (PDF فقط)

### 3. لوحة التحكم (Filament Admin)
- **Dashboard**: إحصائيات شاملة
- **إدارة الشركات**: عرض وتعديل الشركات
- **إدارة الطلبات**: مراجعة وموافقة/رفض الطلبات
- **إدارة اللجان**: إنشاء وإدارة اللجان
- **إدارة المستخدمين**: إضافة وتعديل المستخدمين
- **إعدادات النظام**: شعار، لون، SMTP، إلخ

### 4. نظام Workflow
- **عضو اللجنة**: قبول/رفض مبدئي
- **رئيس اللجنة**: قبول/رفض نهائي
- إرسال بريد إلكتروني تلقائي عند كل مرحلة

### 5. طباعة PDF
- تقارير PDF للطلبات
- تصميم أزرق رسمي

## 🔐 الأدوار والصلاحيات

### Super Admin
- وصول كامل لجميع الميزات
- إدارة المستخدمين والأدوار
- إعدادات النظام

### Admin
- إدارة الشركات والطلبات
- إدارة اللجان
- إدارة المستخدمين

### Committee Member
- مراجعة الطلبات
- قبول/رفض مبدئي

## 📁 هيكل المشروع

```
app/
├── Enums/              # Enums للأنواع المختلفة
├── Filament/           # Filament Resources و Pages
├── Http/
│   ├── Controllers/    # Controllers
│   └── Requests/       # Form Requests
├── Mail/               # Mailables
├── Models/             # Eloquent Models
├── Policies/           # Authorization Policies
└── Services/           # Business Logic Services

database/
├── migrations/         # Database Migrations
└── seeders/            # Database Seeders

resources/
├── views/
│   ├── landing.blade.php
│   ├── company-registration.blade.php
│   ├── pdf/            # PDF Templates
│   └── emails/         # Email Templates
```

## 🔧 الإعدادات المهمة

### إعدادات البريد الإلكتروني (SMTP)

اذهب إلى `/admin/system-settings` وقم بإدخال:

- SMTP Host
- SMTP Port
- SMTP Username
- SMTP Password
- SMTP Encryption (TLS/SSL)

### إعدادات النظام

- **شعار النظام**: رفع شعار المؤسسة
- **اللون الأساسي**: تعديل درجة اللون الأزرق
- **GIF التحميل**: رفع GIF مخصص للتحميل
- **تفعيل التسجيل**: تفعيل/تعطيل تسجيل الشركات
- **مدة صلاحية التأهيل**: عدد الأيام الافتراضي (365 يوم)

## 📝 ملاحظات مهمة

1. **الملفات**: جميع الملفات المرفوعة يجب أن تكون PDF فقط
2. **رقم الهاتف**: يجب أن يبدأ بـ 091، 092، 093، أو 094 متبوعاً بـ 7 أرقام
3. **Soft Deletes**: جميع عمليات الحذف هي Soft Deletes (تعطيل)
4. **اللجان**: يمكن تفعيل لجنة واحدة فقط من كل نوع في نفس الوقت

## 🐛 استكشاف الأخطاء

### مشكلة في الصلاحيات
```bash
php artisan permission:cache-reset
```

### مشكلة في التخزين
```bash
php artisan storage:link
chmod -R 775 storage
chmod -R 775 bootstrap/cache
```

### مشكلة في Cache
```bash
php artisan config:clear
php artisan cache:clear
php artisan view:clear
php artisan route:clear
```

## 📞 الدعم

للمزيد من المعلومات، يرجى زيارة موقع المؤسسة الوطنية للنفط: [noc.ly](https://noc.ly/)

## 📄 الترخيص

حقوق النشر محفوظة © 2025 المؤسسة الوطنية للنفط
